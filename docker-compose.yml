version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
    working_dir: /app
    command: >
      uvicorn main:app
      --host 0.0.0.0
      --port 8080
      --reload
    env_file:
      - docker.env
